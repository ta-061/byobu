## What & why

<!-- What does this change, and why? Link an issue if there is one. -->

## Checklist

- [ ] Scoped to one change (no unrelated refactors bundled in)
- [ ] `python -m unittest discover -s tests -v` passes
- [ ] Added/extended a test for this change
- [ ] Updated `docs-src/` and/or `README.md`/`README.ja.md` if this changes the public API, CLI flags, or config
- [ ] If this touches the web UI, describe how you tested it (manual browser check — no browser test suite yet)
